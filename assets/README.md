# assets

슬라이드에 넣을 이미지(프로그램 스냅샷·사진·로고)를 여기에 둔다.

콘텐츠 JSON 에서 `{"type": "image", "src": "assets/<파일명>", ...}` 로 참조한다.
`tools/blocks.py` 가 원본 종횡비를 유지해 배치하므로 이미지가 늘어나거나 찌그러지지 않는다.

스냅샷은 원본 해상도가 클수록 좋다. 표시 크기 기준 110dpi 미만이면 인쇄·빔프로젝터에서
뭉개진다. 빌드할 때 실제 dpi 가 로그로 출력되므로 넣기 전에 확인할 수 있다.
